@model IEnumerable<LibraryWebApp.Models.Favourite>

@{
    ViewData["Title"] = "Index";
}

<h2>Favourites</h2>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Comment)
            </th>
            <th>
                Link
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Comment)
            </td>
            <td>
                <a asp-action="ItemDetails" asp-route-id="@item.ItemIndex">Link to Item</a>
            </td>
            
            <td>
                <a asp-action="FavouriteEdit" asp-route-id="@item.FavouriteId">Edit</a> |
                <a asp-action="FavouriteDetails" asp-route-id="@item.FavouriteId">Details</a> |
                <a asp-action="FavouriteDelete" asp-route-id="@item.FavouriteId">Delete</a>
               
            </td>
        </tr>
}
    </tbody>
</table>
